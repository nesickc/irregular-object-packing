# Python Compatibility and C++ Deviations

This catalog records questionable Python behaviors that the C++ port intentionally preserves and defects it deliberately corrects. It prevents future maintainers from “cleaning up” compatibility code without understanding why it exists.

The Python implementation remains the reference until the parity milestone is complete.

## Policy

Preserve behavior observed during successful Python runs. Correct a defect when the correction is evident and no meaningful downstream behavior is expected to rely on the defect. When impact is uncertain, preserve the behavior first and review it after parity evidence exists.

Every cataloged behavior must use one of these source markers:

```cpp
// COMPATIBILITY(IROP-COMPAT-NNNN):
// Intentionally preserves questionable Python behavior.
// See docs/COMPATIBILITY.md.
```

```cpp
// DEVIATION(IROP-DEV-NNNN):
// Corrects a Python defect with no expected downstream dependency.
// See docs/COMPATIBILITY.md.
```

`COMPATIBILITY` means C++ intentionally mirrors the questionable behavior. `DEVIATION` means C++ intentionally differs. IDs are permanent and must not be reused.

## Initial Catalog

| ID | Kind | Python behavior | Planned C++ handling | Rationale | Target | Status |
| --- | --- | --- | --- | --- | --- | --- |
| IROP-COMPAT-0001 | Preserved | The current scale barrier is passed as `max_scale`, but the local NLP receives no finite upper scale bound. The returned scale is multiplied into the previous scale and clamped after solving. | Preserved through Milestone 5, then retired by ADR-0012 after a known-feasible full-scale run exhausted 1,000 Ipopt iterations. The replacement is recorded by `IROP-DEV-0023`. | Practical evidence showed that optimizing discarded growth materially harmed termination. The exact barrier remains guarded by independently checked clamp/snap behavior. | Milestone 4; retired Milestone 6 | Retired |
| IROP-COMPAT-0002 | Preserved | CAT collisions are reported, but only object-object and object-container collisions contribute object IDs to scale correction. | Preserve, expose CAT violations in the run result, and mark the correction-selection code. | Adding CAT violations to correction can materially change convergence and final placements. A deterministic CAT-only fixture verifies that the contact remains diagnostic without changing either transform or correction counters. | Milestone 5 | Verified |
| IROP-COMPAT-0003 | Preserved | `PyVista.center_of_mass()` without scalar weights centers the source at its point/vertex centroid rather than its enclosed-volume centroid. | Center at the vertex centroid and persist the original pivot in `placements.json`. | Changing the pivot changes every serialized translation and rotation result. | Milestone 2 | Verified |
| IROP-COMPAT-0004 | Preserved | When the expected object count is one, setup emits volume scale plus zero rotation/translation without consuming random draws. | Preserve the origin transform when the actual transformed surface is strictly contained; use the bounded sampler only when it is invalid. | The successful shortcut is observable in transforms and RNG state. A bounding-sphere-only test would incorrectly randomize slender objects that fit. | Milestone 2 | Verified |
| IROP-COMPAT-0005 | Preserved | CAT supplies `switches="O0/0Q"` together with `cdt=True`, `steinerleft=0`, and other TetGen keyword arguments. The nonempty switch string makes the Python wrapper bypass those keywords and tetrahedralize the combined point set without the advertised PLC/CDT switches. | Pass the literal `O0/0Q` string to TetGen, retain participant facets just as the wrapper does, and regression-test mixed-owner tetrahedra across separated participant surfaces. | Adding `p`, `D`, `z`, or `S0` would silently change the successful CAT cell complex and therefore its constraints. Preserve the live point-union behavior until comparative packing evidence supports a deliberate correction. | Milestone 3 | Verified |
| IROP-COMPAT-0006 | Preserved | The local NLP composes its incremental rotation matrix with the already transformed vertex geometry, but result application adds the three returned Euler components to the stored angles. General Euler addition is not the same matrix composition. | Preserved through Milestone 5, then retired by ADR-0012 after the mismatch rejected a known-feasible two-object result. The replacement is recorded by `IROP-DEV-0022`. | Practical evidence established that additive publication can make a feasible solver candidate infeasible solely through representation drift. | Milestone 4; retired Milestone 6 | Retired |
| IROP-COMPAT-0007 | Preserved | The local translation-delta bound is the configured base distance multiplied by the current volume-scale barrier, rather than its cube root. | Preserve the barrier multiplication and expose the resolved base distance in the run summary. | Although dimensionally surprising, changing the bound can materially alter optimized transforms and convergence. | Milestone 5 | Implemented |
| IROP-DEV-0001 | Corrected | `reduce_all_scales()` iterates over the integer object count, causing a failure on the tetrahedralization recovery path. | Iterate over every valid object index and bound the recovery attempts through the scale-step iteration limit. | The current behavior is an execution defect rather than a meaningful result. A deterministic two-recovery fixture verifies per-object cumulative reduction, progress phases, and the completed result. | Milestone 5 | Verified |
| IROP-DEV-0002 | Corrected | Optimizer history storage and its index are class-level mutable fields, allowing state to leak between optimizer instances. | Store configuration, random state, transforms, and future history per packing-engine instance. | Cross-run state leakage is unintended and unsafe; isolated runs are the evident intent. | Milestone 2 | Implemented |
| IROP-DEV-0003 | Corrected | `SimConfig.new_cat` has a trailing comma and therefore defaults to a one-element tuple instead of a Boolean. | Omit the unused option from the project-owned configuration after reference confirmation. | The tuple is defective and the live initialization path does not consume the option. | Milestone 2 | Verified |
| IROP-DEV-0004 | Corrected | Collision correction repeats until no selected violation remains, without an iteration or time bound. | Add configurable correction and time limits. Return a structured unsuccessful outcome when exhausted. | Preventing nontermination is required for a safe local tool and does not alter successful runs that converge within the limit. Deterministic fixtures verify converged correction and exact limit/accounting behavior. | Milestone 5 | Verified |
| IROP-DEV-0005 | Corrected | The optional grid-spacing objective returns the negative absolute packing-volume error and is minimized, which rewards larger error; the annotated scalar function also returns the optimizer's array. | Omit the dead grid initializer and port the live uniform-rejection path. | The defective grid path is explicitly unused and has no successful live-run behavior to preserve. | Milestone 2 | Verified |
| IROP-DEV-0006 | Corrected | Initialization and local-solver guesses seed and mutate NumPy's process-global legacy MT19937 state. | Give each run a local MT19937 stream with NumPy-compatible 53-bit uniform mapping and persisted seed/draw count, and continue that stream for local guesses. | Independent runs must not perturb each other; the mapping preserves seeded successful results. | Milestone 2 and 5 | Verified |
| IROP-DEV-0007 | Corrected | The primary coordinate rejection loop has no intrinsic attempt bound; a timeout wrapper is used only for a size heuristic. | Enforce configurable candidate, geometry-query, pairwise-distance, and exact-surface work limits. The reference greedy bounding-sphere search remains the first phase; after its candidate bound, Milestone 7 adds the separately bounded DEV-0026 fallback. Disable that fallback for the historical failure oracle. | Bounded work prevents nontermination and algorithmic denial of service without changing successful runs inside the limits. Exhaustion reports bounded search failure, not geometric infeasibility. Successful reference seeds and their RNG/work remain preserved; the former post-parity deferral is fulfilled by DEV-0026. | Milestone 2; parity review Milestone 6 | Verified |
| IROP-DEV-0008 | Corrected | Initial-state validation checks surface contacts and can miss wholly outside or nested nonintersecting objects. | Require the generated bounding sphere to be strictly inside; for the compatible one-object origin shortcut, require strict vertex containment plus no object/container surface intersection. | A success scene must not place geometry outside the container. | Milestone 2 | Verified |
| IROP-DEV-0009 | Corrected | The one-object shortcut emits the origin transform even when it is invalid and then fails setup. | Fall back to the normal bounded sampler when the actual origin placement is not contained. | A valid alternative placement is preferable to a deterministic invalid scene. | Milestone 2 | Verified |
| IROP-DEV-0010 | Corrected | Adaptive sampling truncation can request fewer than four triangles. | Retain at least four triangles, the minimum for a closed triangular volume. | Smaller targets cannot represent a safe closed surface. | Milestone 2 | Verified |
| IROP-DEV-0011 | Corrected | Manifold meshes with locally inconsistent triangle winding pass the Python gate, but signed proximity can depend on unreliable face normals. | Normalize winding in the geometry query's owned snapshot and leave the caller mesh unchanged. | Deterministic orientation makes volume and containment queries reliable without narrowing successful single-surface inputs. | Milestone 2 | Verified |
| IROP-DEV-0012 | Corrected | Python accepts multi-component closed surfaces without distinguishing disjoint solids from nested cavity shells. | Reject multiple closed surface components until cavity semantics are explicitly modeled. | Treating every component as positive volume can classify a cavity as usable packing space; silent mispacking is unsafe. | Milestone 2 | Verified |
| IROP-DEV-0013 | Corrected | The Python tetrahedralization and CAT path trusts point ownership, output point ordering, tetrahedron indices, finite coordinates, and nondegenerate cells after only a point-count assertion. | Validate project meshes before TetGen, require unchanged output point order before assigning owners, and reject invalid CAT ownership, indices, non-finite coordinates, and degenerate tetrahedra with project-owned status values. | The unchecked behavior can misassign participants, create NaN plane normals, or access unrelated array elements; no successful result depends on accepting structurally unsafe geometry. | Milestone 3 | Verified |
| IROP-DEV-0014 | Corrected | Python places no configured count or generated-work limits around tetrahedralization inputs/accepted outputs or CAT relevant cells, polygons, polygon vertices, and constraints. | Enforce configurable pre-adapter counts, accepted TetGen output counts, and CAT examination/output limits; report resource exhaustion with work counters. | Explicit limits bound project-controlled memory and algorithmic work without changing successful cases inside the configured limits. TetGen's own in-process peak work remains a documented residual risk. | Milestone 3 | Verified |
| IROP-DEV-0015 | Corrected | The Python Ipopt path discards the backend status, while the SLSQP fallback ignores `success`; either iterate is applied unconditionally without a finite or feasibility check. | Translate Ipopt outcomes into project-owned statuses and publish an accepted transform only after successful/acceptable termination plus independent solver-space and actual-applied-geometry checks. | Failed, non-finite, out-of-bounds, or infeasible iterates must not corrupt packing state. Successful reference results remain unchanged inside tolerance. | Milestone 4 | Verified |
| IROP-DEV-0016 | Corrected | The Python local solve assumes nonempty, correctly shaped finite constraint arrays and leaves optimizer work, dense derivative allocation, and termination effectively unbounded. | Validate ownership, ranges, finite geometry, unit normals, positive scale, bounds, and an explicit initial guess; reject empty/singular problems and enforce configurable constraint, dense-Jacobian, callback-work, iteration, and elapsed-time limits. | Local mesh/configuration data is untrusted. Structured rejection and bounded project-controlled work avoid unsafe dependency calls and nontermination without changing valid solves inside the limits. | Milestone 4 | Verified |
| IROP-DEV-0017 | Corrected | The default threaded path does not consume or await `ThreadPoolExecutor.map()`, so collision handling races solver writes and process-global random draws. | Solve deterministically in object-ID order into a separate candidate state and commit the complete batch only after every solve succeeds. | Waiting and transactional publication remove scheduling-dependent state corruption; no valid algorithm result depends on the race. | Milestone 5 | Implemented |
| IROP-DEV-0018 | Corrected | Exhausting a barrier iteration count silently advances to the next barrier, interruption is swallowed, and natural return is treated as completion without a final success predicate. | Stop with a structured unsuccessful status on iteration exhaustion or engine cancellation, poll interruption through input preparation, initialization, engine work, and successful publication, and report success only after the final barrier and final validation pass. Pre-engine cancellation exits 130 without a run directory; engine or post-success cancellation publishes summary-only. On Windows, register SIGBREAK against the same cancellation flag as SIGINT; a child-process harness sends targeted `CTRL_BREAK_EVENT` and verifies exit 130 with summary-only publication. | A partial or interrupted run must not be presented as a successful packing. | Milestone 5 | Verified |
| IROP-DEV-0019 | Corrected | Sampled, surface-only collision checks with a fixed `1e-6` cell tolerance can miss wholly outside objects and nested overlaps, and no full-resolution final validation exists. | Use bounded exact surface-contact checks plus strict solid containment/nesting tests on full-resolution geometry for correction and final acceptance; retain the exact-predicate difference in this record. | Success artifacts need an explicit physical validity invariant. Exact project-owned predicates avoid accepting hidden overlap while making the near-gap policy deterministic in input units. | Milestone 5 | Verified |
| IROP-DEV-0020 | Corrected | Adaptive downsampling tries Trimesh quadric decimation before a PyVista fallback, while upsampling, smoothing, and cleaning use PyVista/VTK without validating the resulting closed surface. | Use the pinned VTK quadric-decimation/Loop-subdivision/smoothing pipeline privately, preserve the target-count formulas, and revalidate a single closed output surface. | Keeping one pinned dependency path is reproducible within the C++ build and avoids accepting invalid intermediate geometry; exact triangulation is not a stable reference contract. | Milestone 5 | Verified |
| IROP-DEV-0021 | Corrected | When the translation setting is `None`, the Python local solve effectively has no finite translation bound. | Resolve an omitted C++ base bound to twice the cube root of the full-size object volume, retain an explicitly configured positive bound, multiply the resolved base by the current barrier as recorded by `IROP-COMPAT-0007`, and persist both the configured and resolved values in `run-summary.json`. | A finite object-size-derived default bounds local search without imposing a fixed coordinate unit, improves reproducibility and termination safety, and avoids changing an explicit user choice. | Milestone 5 | Verified |
| IROP-DEV-0022 | Corrected | The constraint model applies an incremental rotation to already transformed geometry as `R_delta * R_current`, while Python persists componentwise Euler addition. | Left-compose the two rotation matrices, encode the result canonically in the existing `Ry * Rz * Rx` Euler representation, and independently postcheck the exact stored transform, including at gimbal lock. | The stored transform must represent the orientation that the accepted constraints evaluated; additive Euler drift caused a known-feasible two-object run to fail. | Milestone 6 | Verified |
| IROP-DEV-0023 | Corrected | Python leaves the local scale objective without a finite upper barrier, then clamps the returned multiplier after solving. Near an exact barrier, this spends unbounded solver work on growth that is discarded. | Supply an overflow-safe finite bound with fixed representable slack near the barrier, saturate extreme ratios below the adapter sentinel, retain exact clamping, and independently postcheck a near-target exact-scale snap before accepting it. | The bounded objective reduced the reported full-scale case from iteration-limit failure to two successful solves while retaining exact target and physical feasibility; saturation and snap tests cover extreme and near-tight cases. | Milestone 6 | Verified |
| IROP-DEV-0024 | Corrected | Python checks whether every object reached the active barrier only after resampling, TetGen/CAT construction, local optimization, and collision handling. An already-complete state can therefore be scaled down by recovery or fail redundant dependency work. | Before barrier-specific sampling and optimization, mark an already-satisfied barrier complete without changing transforms, RNG state, or iteration history; retain mandatory final full-resolution and serialized-output validation. | Redundant work must not turn a physically valid completed packing into failure or alter its placements. | Milestone 6 | Verified |
| IROP-DEV-0025 | Corrected | The Python mesh conversion helper passes `algorithm=None` to `extract_surface()`, but the pinned reference PyVista 0.38.4 API has no `algorithm` parameter and raises `TypeError` before the live path can run. | Omit the unsupported default-valued keyword and call `extract_surface()` with its normal PyVista 0.38.4 behavior before triangulation. | Removing an argument that the pinned API cannot accept restores the executable reference path without changing the requested extraction behavior. The live Python parity oracle and complete Python test suite verify the correction. | Milestone 6 | Verified |
| IROP-DEV-0026 | Corrected | The greedy reference initializer can remain jammed despite feasible dense layouts. | After bounded random candidate exhaustion, try a deterministic six-orientation AABB grid using shared work budgets and strict actual containment/envelope separation; retain an explicit disable option and all successful reference transforms/RNG behavior. Report the method and reference/grid work separately. | Bounded heuristic exhaustion does not prove infeasibility. Generated and supplied ten/36-cylinder scenes succeed; focused seed/RNG, concavity, forged-state, resource and cancellation regressions pass in the 192-test Debug/Release/Ninja clang-tidy matrices; the live Python parity oracle also passes. | Milestone 7 | Verified |
| IROP-DEV-0027 | Corrected | Python collision traversal has no explicit object-pair work bound. | Skip strictly disjoint validated object AABBs and bound every enumerated pair cumulatively across packing and output validation; retain exact contact/nesting checks and report order. | Avoiding narrow-phase work must not permit unbounded quadratic pair scanning. Strict-contact/nesting and cumulative engine/output-budget regressions pass in the 192-test Debug/Release/Ninja clang-tidy matrices. The recorded separated-scene benchmark retains identical collision outcomes and pair order. | Milestone 7 | Verified |

| IROP-DEV-0028 | Corrected | Python starts every local problem with a small multiplicative scale and random rotation, including objects already at the current barrier. Shrinking can move vertices outside their CAT halfspaces. | Default growth starts from the unchanged current pose and retains completed object poses. The explicit reference-growth policy keeps old guesses and work; actual solves retain deterministic ordered random draws. | The captured ten-object failure has 422 initially violated rows under the reference guess and none at the current pose; changing only the guess makes replay succeed in 80 iterations instead of exhausting 1,000. | Improvement tranche 2 | Verified; see STATUS.md |
| IROP-DEV-0029 | Corrected | The previously corrected C++ barrier bound still adds slack, then clamps the applied scale. Shrinking a feasible optimum can violate per-vertex CAT halfspaces. | Default growth uses the exact finite target/current upper bound, with the existing overflow guard and independently validated exact-target snap. Reference mode retains ADR-0012 slack. | A second captured failure passes with the exact bound and unchanged postchecks; the original clamp loses about 0.000209 units of constraint clearance. | Improvement tranche 2 | Verified; see STATUS.md |
| IROP-DEV-0030 | Corrected | The Python Ipopt path uses a limited-memory Hessian approximation for the seven-variable local problem. | Default growth uses an analytic weighted constraint Hessian; standalone requests and old snapshots preserve the approximation unless explicitly enabled. Hessian rows share the existing cumulative constraint-work budget and all acceptance checks remain mandatory. | Controlled same-DLL experiments reduce the original bad-start failure from 1,000 iterations to 38 and identity-start work from 80 to 27, without changing tolerances. | Improvement tranche 2 | Verified; see STATUS.md |
| IROP-DEV-0031 | Corrected | Adaptive surface approximations can omit geometry and repeatedly grow into full-resolution collisions corrected by scale reduction. | After physical correction in adaptive default growth, double the object triangle target up to the original count, retain that floor across barriers and keep the container schedule independent. Reference mode preserves the old sampling schedule; all work/validation bounds remain. | Full-resolution correction exposes approximation error; repeated growth against unchanged coarse surfaces must not be the only recovery path. Success still requires physical and serialized-output validation. | Improvement tranche 2 | Verified; see STATUS.md |
| IROP-DEV-0032 | Corrected | Diagnostic CAT contacts share the physical collision triangle/report budgets and can stop otherwise valid physical checks. | Give CAT contacts a separate bounded allowance and explicit completeness metadata; validate all CAT inputs and preserve every physical check. The packing engine uses a measured one-billion physical triangle allowance with the same 300-second deadline; standalone queries retain 100 million. | Measured ten-object diagnostics exhaust even a one-billion-pair experimental shared budget. Incomplete diagnostics are distinct from physical validity; interrupted CAT-bearing physical queries cannot certify completeness when their report is lost. | Improvement tranche 2 | Verified; see STATUS.md |
| IROP-DEV-0033 | Corrected | Strict C++ output validation rejects even single-participant TetGen cells whose tiny determinant rounds to zero, although CAT discards all such internal cells. | Default packing can omit only finite zero-determinant, distinct/range-valid, single-participant cells; retain strict standalone/reference behavior, raw output bounds and an omission counter. | Exact arithmetic on measured rejected cells yields tiny positive volumes; they contribute no CAT constraint. Mixed-participant/nonfinite/invalid-index failures remain strict. | Improvement tranche 2 | Verified; see STATUS.md |
| IROP-DEV-0034 | Corrected | Reference container Loop subdivision and smoothing can substantially shrink the optimization container. | Default growth uses double midpoint subdivision without moving existing vertices, smoothing or decimation, and retains the first active barrier's container sites as objects grow. Explicit reference mode retains the historical schedule. | The supplied box proxy retained only 37.14% of the original volume at 3,072 triangles. Surface-preserving subdivision uses existing mesh/subdivision budgets and unchanged physical gates. | Improvement tranche 2 | Verified; see STATUS.md |
| IROP-DEV-0035 | Corrected | Full-surface local trials can repeatedly collide and lose progress through fixed scale correction. | Before shrinking a rejected full-input trial, default growth can halve the offending unfinished objects' motion bounds and cap their absolute volume increment, up to four levels per object per barrier. Re-solve from the previous valid scene; retain all work/deadline bounds and strict acceptance. | The supplied case cycles with eight full-size objects and two incomplete objects at the final barrier, despite successful local solves. Rejected batch transforms and RNG are never committed. | Improvement tranche 2 | Verified; see STATUS.md |
| IROP-DEV-0036 | Corrected | The reference relies on ambient numerical-library thread defaults for every small local solve. | Packing requests one task-scoped OpenMP thread by default, with explicit zero for inherited behavior; restore caller state and record MKL overrides separately. Historical standalone snapshots retain inherited settings. | Measured replays and complete growth show substantial overhead from the pinned parallel numerical runtime. Full-run floating-point paths may differ, so exact target and physical validation remain authoritative. | Improvement tranche 3 | Verified for registered scope; see STATUS.md and ADR-0017 |
| IROP-DEV-0037 | Corrected | An immediate physical-step retry rebuilds unchanged TetGen/CAT geometry and re-solves every unfinished object after rejecting the complete trial. | Default full-input growth retains one rejected batch context and successful local transforms; invalidate each transform when its bounds change and discard all reuse on commit or any context transition. Preserve ordered logical requests and RNG draws, full candidate validation and all cumulative limits. Explicit disable and reference growth retain recomputation. | In controlled 100/300-object profiles, rejected batches account for 57-65% of local calls. Reuse reduces actual work without accepting an unvalidated trial or weakening physical predicates. | Improvement tranche 3 | Verified for registered scope; see STATUS.md and ADR-0017 |

## Scale Semantics

The transform array stores a volume scale. Mesh transformation applies the cube root as the linear scale. This is intentional algorithm behavior, not a deviation. Name C++ fields accordingly so it is not mistaken for a linear scale.

## Adding an Entry

Before adding or changing compatibility behavior:

1. Identify the exact Python source and triggering conditions.
2. State whether the behavior occurs on successful runs or only failure paths.
3. Assess possible downstream effects on transforms, convergence, output geometry, and persisted state.
4. Prefer preservation when effects are uncertain.
5. Assign the next permanent ID.
6. Add the code marker and focused regression test.
7. Update the entry status and cite verification in `docs/STATUS.md`.

## Status Values

- `Planned`: documented before C++ implementation.
- `Implemented`: the C++ behavior and code marker exist.
- `Verified`: focused tests and, when relevant, Python/C++ comparison evidence pass.
- `Retired`: behavior was intentionally removed through a later decision; retain the historical entry and link its ADR.
